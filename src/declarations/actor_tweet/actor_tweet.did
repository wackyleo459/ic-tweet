type User = 
 record {
   email: opt text;
   id: principal;
   name: opt text;
   userId: opt text;
 };
type Time = int;
type Post = 
 record {
   author: User;
   date: Time;
   message: opt text;
 };
service : {
  addPost: (Post) -> (text);
  getAll: () -> (vec nat);
}
